CREATE DATABASE test_connect;

CREATE TABLE connect_sample01(
                                 id SERIAL,
                                 name VARCHAR(100),
                                 text VARCHAR(100),
                                 primary key (id)
);